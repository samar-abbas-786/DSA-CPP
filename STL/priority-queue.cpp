#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    // priority_queue<int, vector<int>, greater<int>> pq;
    // priority_queue<int> pq;
    //       //push & Pop --> O(logn)
    //       //top --> O(1)
    // pq.push(4);
    // pq.push(3);
    // cout << pq.top();

    // set<int> st;

    // st.insert(3);
    // st.insert(2);
    // st.insert(5);
    // auto it = st.find(6);    ---> It Point to st.end() if not find the element
    // it--;
    // cout<<*it;  // It will print --> 5

    // multiset<int>ms  --> It will store in order but not unique unlike set

    // ms.erase(1) --> it will erase all the 1
    // ms.erase(ms.find(1)) --> it will erase only first occurrence of 1

    // unordered_set<int> ust;  --> Lower bound and Upper bound does not work  --> Unique but not sorted
    // All the operations  --> O(1)

    //Map

    map<int,int>mpp

    return 0;
}